# P1.4 — Evolução UX landing + alinhamento territorial (aplicado)

## Landing (`/p/:slug`)

- Hero com foto (`photo_url`), headline, bio, CTAs **Quero apoiar** + WhatsApp
- Fluxo principal **antes** de demanda e chapas
- Formulário de apoio com borda primária e copy de conversão
- CEP com narrativa **território reconhecido**; bairro/cidade ocultos após confirmação
- Chapas em `<details>` opcional (sem jargon de “votos na meta” no fluxo principal)
- Demanda em `<details>` secundário, com `TerritoryChip` quando território confirmado
- Footer Strategos discreto

## P1.3c — Relatórios

Migration: `20260620120000_p13c_reports_territory_keys.sql`

- `demand_matches_neighborhood_filter`
- `get_tenant_reports_summary` com `territory_key` + filtros canônicos

## Dashboard

- Seção analítica renomeada: **Cenário eleitoral (pesquisa manual)** + disclaimer
- Links do mapa territorial usam `territoryLabel`

## CRM

- Filtro `bairro` em Eleitores via `territoryFilterMatches`
- Componente compartilhado `TerritoryChip`

## Deploy

```bash
supabase db push   # inclui 20260620120000 se ainda não aplicada
```

Frontend: deploy estático habitual (Vite build).
