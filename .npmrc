# Permite postinstall de dependências nativas (Vite, Cloudflare Workers)
# Necessário no pnpm 10+ — ver pnpm-workspace.yaml > allowBuilds

# TanStack: expõe query-core para o Rollup/Vite no CI (pnpm isolado)
public-hoist-pattern[]=@tanstack/*
